{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Debug Executable",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/bin/TemplateProject",
            "cwd": "${workspaceFolder}"
        }
    ]
}